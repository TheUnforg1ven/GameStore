<div>
    <h2>All games shown here</h2>
</div>

<h2>@Model.CurrentCategory</h2>

<div class="row">
    @foreach (Game game in Model.Games)
    {
        @await Html.PartialAsync("GameSummary", game)
    }
</div>
